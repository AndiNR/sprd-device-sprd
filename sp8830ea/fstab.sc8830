# Android fstab file.
# The filesystem that contains the filesystem checker binary (typically /system) cannot
# specify MF_CHECK, and must come before any filesystems that do specify MF_CHECK
# <src>                        <mnt_point>     <type>    <mnt_flags and options>               <fs_mgr_flags>

/dev/block/mmcblk0p1          /system          ext4    ro                                     wait 
/dev/block/mmcblk0p4	       /data            ext4    noatime,nosuid,nodev,nomblk_io_submit  wait,encryptable=footer
/dev/block/mmcblk0p2	       /cache           ext4    noatime,nosuid,nodev,nomblk_io_submit  wait
/dev/block/mmcblk0p3	       /productinfo           ext4    noatime nosuid nodev nomblk_io_submit  wait
